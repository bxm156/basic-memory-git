# Test Document

This is a test document for integration testing.

## Overview

The basic-memory-git project syncs markdown files from a git repository and makes them available via MCP.

## Key Features

- Automated git sync
- Semantic search via basic-memory
- SSE transport via supergateway
